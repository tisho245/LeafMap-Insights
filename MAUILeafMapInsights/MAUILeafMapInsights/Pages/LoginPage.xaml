<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUILeafMapInsights.Pages.LoginPage"
             Title="Вход">
    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="12">
            <Label Text="Вход" FontSize="Title" />
            <Entry x:Name="EmailEntry" Placeholder="Email" Keyboard="Email" />
            <Entry x:Name="PasswordEntry" Placeholder="Парола" IsPassword="True" />
            <Label x:Name="ErrorLabel" TextColor="Red" IsVisible="False" />
            <Button Text="Вход" Clicked="OnLoginClicked" />
            <Button Text="Регистрация" Clicked="OnGoRegisterClicked" BackgroundColor="Transparent" TextColor="{StaticResource Primary}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
